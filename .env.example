# Local dev uses a file-based SQLite database by default (file:local.db).
# For production, point these at a Turso database (https://turso.tech):
# DATABASE_URL="libsql://your-db.turso.io"
# DATABASE_AUTH_TOKEN="your-token"

DATABASE_URL="file:local.db"
